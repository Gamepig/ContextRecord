name: New mcpServer
version: 0.0.1
schema: v1
mcpServers:
  - name: mcp-memory-bank
    command: uvx
    args:
      - "--from"
      - "git+https://github.com/ipospelov/mcp-memory-bank"
      - "mcp_memory_bank"
    env: {}
